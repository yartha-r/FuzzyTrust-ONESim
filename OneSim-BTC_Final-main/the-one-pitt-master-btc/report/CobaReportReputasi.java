/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package report;

/**
 *
 * @author gregoriusyuristamanugraha
 */
import btc.Incentive;
import core.DTNHost;
import java.util.Map;

public class CobaReportReputasi extends Report {

    public CobaReportReputasi() {
        init();
    }

    @Override
    protected void init() {
        super.init();
    }

    @Override
    public void done() {
////        Map<DTNHost, Map<DTNHost, Double>> inctv = Incentive.reputation;
//        String cetak = "ID Verificator,Node,Rep\n";
//        for (Map.Entry<DTNHost, Map<DTNHost, Double>> entry : inctv.entrySet()) {
//            DTNHost verificator = entry.getKey();
//            for (Map.Entry<DTNHost, Double> repList : inctv.get(verificator).entrySet()) {
//                cetak += verificator + "," + repList.getKey() + "," + repList.getValue() + "\n";
//            }
//        }
//        write(cetak);
//        super.done();
    }
}
